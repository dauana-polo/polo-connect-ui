import { useState } from "react";
import { Building2, CheckCircle2, Circle, FilePlus2, FileText, Plus, QrCode, ReceiptText, Upload, Users } from "lucide-react";
import { toast } from "sonner";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

const sectors = [
  { name: "Jurídico", detail: "Contrato em revisão", progress: "Em andamento" },
  { name: "Logística", detail: "Aguardando emissão aérea", progress: "Atenção" },
  { name: "Financeiro", detail: "1ª parcela recebida", progress: "Em dia" },
  { name: "Faturamento", detail: "NF pendente de emissão", progress: "Pendente" },
];

export function PostSalePrototype() {
  const [tasks, setTasks] = useState([true, false, false]);
  return <div className="mb-4 rounded-md border border-primary/20 bg-primary/5 p-4"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-semibold">Controle geral do Pós-venda</h2><Badge variant="outline">Protótipo</Badge></div><p className="mt-1 text-xs text-muted-foreground">Visão alinhada de Jurídico, Logística, Financeiro e Faturamento.</p></div><Dialog><DialogTrigger asChild><Button><FileText className="h-4 w-4" /> Abrir dossiê operacional</Button></DialogTrigger><DialogContent className="max-h-[92vh] max-w-5xl overflow-y-auto"><DialogHeader><DialogTitle>Dossiê operacional — Convenção Nexa 2026</DialogTitle><p className="text-sm text-muted-foreground">Todos os campos abaixo são demonstrativos e não salvam dados.</p></DialogHeader><Tabs defaultValue="visao"><TabsList className="h-auto w-full justify-start overflow-x-auto"><TabsTrigger value="visao">Visão geral</TabsTrigger><TabsTrigger value="envolvidos">Envolvidos</TabsTrigger><TabsTrigger value="faturamento">Faturamento</TabsTrigger><TabsTrigger value="documentos">Documentos</TabsTrigger><TabsTrigger value="atividades">Atividades</TabsTrigger></TabsList><TabsContent value="visao" className="grid gap-3 pt-4 sm:grid-cols-2">{sectors.map((sector) => <Card key={sector.name}><CardContent className="p-4"><div className="flex items-center justify-between"><strong className="text-sm">{sector.name}</strong><Badge variant="outline">{sector.progress}</Badge></div><p className="mt-3 text-sm text-muted-foreground">{sector.detail}</p></CardContent></Card>)}</TabsContent><TabsContent value="envolvidos" className="grid gap-4 pt-4 lg:grid-cols-3"><PersonCard title="Cliente" values={["Fernanda Lima", "fernanda@nexa.com.br", "(11) 98888-1122"]} /><PersonCard title="Interveniente" values={["Carlos Mendes", "carlos@nexa.com.br", "(11) 97777-3344"]} /><PersonCard title="Testemunha (opcional)" values={["Nome", "E-mail", "Telefone"]} /></TabsContent><TabsContent value="faturamento" className="space-y-4 pt-4"><Card><CardContent className="grid gap-4 p-5 sm:grid-cols-2"><div><Label>Quem fará o faturamento?</Label><Select defaultValue="polo"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="polo">Polo Palestrantes</SelectItem><SelectItem value="pena">Pena</SelectItem><SelectItem value="agencia">Agência parceira</SelectItem><SelectItem value="outro">Outra empresa</SelectItem></SelectContent></Select></div><div><Label>Empresa / agência parceira</Label><Input placeholder="Selecione ou digite" /></div><div><Label>Condição de pagamento</Label><Input defaultValue="50% entrada + 50% D-5" /></div><div><Label>Pedido de compra</Label><Input placeholder="Número ou referência" /></div></CardContent></Card></TabsContent><TabsContent value="documentos" className="space-y-3 pt-4"><div className="grid gap-3 sm:grid-cols-3">{[["Nota fiscal","NF-2038.pdf"],["Contrato","Contrato-assinado.pdf"],["QR Code","Checklist emitido"]].map(([type,name]) => <Card key={type}><CardContent className="p-4"><FileText className="h-5 w-5 text-primary" /><div className="mt-3 text-sm font-semibold">{type}</div><div className="text-xs text-muted-foreground">{name}</div><Badge variant="outline" className="mt-3"><CheckCircle2 className="h-3 w-3" /> Disponível</Badge></CardContent></Card>)}</div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => toast.info("Anexo demonstrativo; nenhum arquivo foi enviado.")}><Upload className="h-4 w-4" /> Anexar documento</Button><Button variant="outline"><QrCode className="h-4 w-4" /> Ver QR Code</Button><Button variant="outline"><ReceiptText className="h-4 w-4" /> Anexar NF</Button></div></TabsContent><TabsContent value="atividades" className="space-y-3 pt-4">{["Contrato pronto até 10 dias antes", "Enviar checklist 7 dias antes do evento", "Confirmar logística 5 dias antes"].map((task,index) => <label key={task} className="flex items-center gap-3 rounded-md border p-3"><Checkbox checked={tasks[index]} onCheckedChange={(checked) => setTasks((old) => old.map((value,i) => i === index ? !!checked : value))} />{tasks[index] ? <CheckCircle2 className="h-4 w-4 text-success" /> : <Circle className="h-4 w-4 text-muted-foreground" />}<span className="flex-1 text-sm">{task}</span><Badge variant="outline">Pré-definida</Badge></label>)}<Button variant="outline" onClick={() => toast.info("Atividade extra demonstrativa adicionada.")}><Plus className="h-4 w-4" /> Atividade extra</Button></TabsContent></Tabs></DialogContent></Dialog></div></div>;
}

function PersonCard({ title, values }: { title: string; values: string[] }) { return <Card><CardHeader><CardTitle className="flex items-center gap-2 text-sm"><Users className="h-4 w-4" />{title}</CardTitle></CardHeader><CardContent className="space-y-2">{values.map((value,index) => <Input key={`${title}-${index}`} defaultValue={value} />)}</CardContent></Card>; }